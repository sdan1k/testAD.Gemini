# FAS Semantic Search Frontend

Веб-интерфейс для семантического поиска по решениям ФАС (Федеральной антимонопольной службы) о нарушениях в рекламе.

## Описание

Современное одностраничное приложение с минималистичным интерфейсом для поиска по базе из 7283 решений ФАС. Использует семантический поиск — система понимает смысл запроса и находит релевантные документы даже без точного совпадения слов.

## Технологии

- **Next.js 16** — React-фреймворк с серверным рендерингом
- **TypeScript** — статическая типизация
- **Tailwind CSS 4** — утилитарный CSS-фреймворк
- **shadcn/ui** — компоненты на основе Radix UI
- **Turbopack** — быстрый сборщик для разработки

## Установка

### Требования

- Node.js 18+
- npm или yarn

### Шаги установки

```bash
# Клонирование репозитория
git clone https://github.com/Latarho/Legal.git
cd Legal/frontend

# Установка зависимостей
npm install
```

## Запуск

### Development режим

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

### Production сборка

```bash
# Сборка
npm run build

# Запуск
npm start
```

## Конфигурация

### Переменные окружения

Создайте файл `.env.local` в папке `frontend/`:

```env
# URL бэкенд API (по умолчанию http://localhost:8000)
NEXT_PUBLIC_API_URL=http://localhost:8000
```

## Структура проекта

```
frontend/
├── src/
│   ├── app/
│   │   ├── globals.css      # Глобальные стили + CSS переменные shadcn
│   │   ├── layout.tsx       # Корневой layout
│   │   └── page.tsx         # Главная страница с поиском
│   ├── components/
│   │   ├── ui/              # shadcn/ui компоненты
│   │   │   ├── badge.tsx
│   │   │   ├── button.tsx
│   │   │   ├── card.tsx
│   │   │   └── input.tsx
│   │   ├── case-card.tsx    # Карточка результата поиска
│   │   └── search-form.tsx  # Форма поиска
│   └── lib/
│       ├── api.ts           # API клиент для бэкенда
│       └── utils.ts         # Утилиты (cn для классов)
├── public/                  # Статические файлы
├── components.json          # Конфигурация shadcn/ui
├── tailwind.config.ts       # Конфигурация Tailwind
├── tsconfig.json           # Конфигурация TypeScript
└── package.json
```

## Компоненты

### SearchForm

Форма поиска с полем ввода и кнопкой.

**Props:**
- `onSearch: (query: string) => void` — колбэк при отправке запроса
- `isLoading: boolean` — состояние загрузки

**Особенности:**
- Примеры запросов для быстрого старта
- Индикатор загрузки
- Валидация пустого запроса

### CaseCard

Карточка с информацией о решении ФАС.

**Props:**
- `caseData: CaseResult` — данные решения
- `rank: number` — позиция в результатах

**Отображает:**
- Процент совпадения (score)
- Тип нарушения (substance/placement)
- Ответчик и отрасль
- Дата решения
- Цитата рекламного контента
- Суть нарушения
- Нарушенные статьи закона
- Тематические теги
- Ссылка на решение ФАС

## API клиент

Файл `src/lib/api.ts` содержит типизированные функции для работы с бэкендом:

```typescript
// Поиск по решениям
const results = await searchCases("текст запроса", 10);

// Проверка состояния сервера
const health = await checkHealth();
```

### Типы

```typescript
interface CaseResult {
  index: number;
  score: number;
  docId: string | null;
  Violation_Type: string | null;
  document_date: string | null;
  defendant_name: string | null;
  violation_summary: string | null;
  // ... и другие поля
}

interface SearchResponse {
  query: string;
  total_cases: number;
  results: CaseResult[];
}
```

## Стилизация

### Цветовая схема

Проект использует CSS-переменные shadcn/ui с поддержкой тёмной темы:

- `--background` / `--foreground` — основные цвета
- `--primary` — акцентный цвет
- `--muted` — приглушённые элементы
- `--destructive` — цвет ошибок
- `--card` — цвет карточек

### Адаптивность

Интерфейс адаптирован для всех размеров экрана:
- Мобильные устройства (< 640px)
- Планшеты (640px - 1024px)
- Десктоп (> 1024px)

## Скрипты

```bash
# Разработка с hot-reload
npm run dev

# Production сборка
npm run build

# Запуск production сервера
npm start

# Линтинг
npm run lint
```

## Особенности

### Индикатор состояния сервера

В шапке отображается статус подключения к бэкенду:
- 🟢 Зелёный — сервер доступен, данные загружены
- 🔴 Красный — сервер недоступен
- 🟡 Жёлтый (мигает) — проверка соединения

### Примеры запросов

Для быстрого старта доступны готовые примеры:
- "Скидка 90% на всё"
- "Лучший банк года"
- "Реклама алкоголя"
- "СМС без согласия"
- "Недостоверная реклама лекарств"

### Обработка ошибок

- Отображение сообщений об ошибках API
- Graceful degradation при недоступности сервера
- Валидация пользовательского ввода

## Лицензия

MIT
